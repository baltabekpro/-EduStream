# Teacher Usage Scenario Testing

## Описание

Этот набор скриптов создан для тестирования платформы EduStream с точки зрения учителя. Скрипты проверяют все основные функции платформы и используют AI модель от OpenRouter для анализа удобства использования.

## Тестируемые сценарии

1. **Аутентификация** - Регистрация и вход учителя
2. **Управление курсами** - Создание и просмотр курсов
3. **Загрузка материалов** - Загрузка учебных материалов
4. **AI Workspace** - Генерация тестов с помощью AI
5. **OCR функционал** - Проверка работ учеников
6. **Дашборд** - Просмотр общей информации
7. **Аналитика** - Просмотр статистики успеваемости
8. **Настройки** - Управление профилем и настройками

## Требования

- Node.js (версия 14 или выше)
- Доступ к API EduStream
- Интернет соединение для вызова OpenRouter AI

## Установка

Клонируйте репозиторий:
```bash
git clone https://github.com/baltabekpro/-EduStream.git
cd -EduStream
```

## Использование

### Вариант 1: JavaScript с реальным API (требуется работающий backend)

```bash
node teacher-usage-test.cjs
```

### Вариант 2: Demo версия (работает без backend, показывает пример)

```bash
node teacher-usage-test-demo.cjs
```

### Вариант 3: TypeScript (для разработчиков)

```bash
# TypeScript версия доступна в teacher-usage-test.ts
# Требуется компиляция перед запуском
```

### Настройка переменных окружения

По умолчанию скрипт использует:
- API URL: `https://94.131.85.176/api/v1`
- OpenRouter Model: `mistralai/mistral-small-3.1-24b-instruct:free`

**ВАЖНО**: Для использования OpenRouter AI необходимо установить API ключ:

```bash
export OPENROUTER_API_KEY="your-api-key-here"
node teacher-usage-test.cjs
```

Вы можете изменить эти настройки через переменные окружения:

```bash
export API_BASE_URL="https://your-api-url.com/api/v1"
export FRONTEND_URL="https://your-frontend-url.com"
export ALLOW_INSECURE_SSL="true"  # Только для разработки!
node teacher-usage-test.cjs
```

**Безопасность**: Никогда не коммитьте API ключи в репозиторий!

```bash
export API_BASE_URL="https://your-api-url.com/api/v1"
export FRONTEND_URL="https://your-frontend-url.com"
node teacher-usage-test.js
```

## Результаты тестирования

Скрипт выводит:

### 1. Результаты по каждому тесту
- ✅ Успешно - функция работает корректно
- ⚠️ Частично - функция работает, но есть проблемы
- ❌ Ошибка - функция не работает

### 2. AI анализ каждого сценария
- Краткий анализ результата
- Оценка удобства использования (1-10)
- Рекомендации по улучшению

### 3. Общая статистика
- Процент успешных тестов
- Средняя оценка удобства
- Общая оценка готовности платформы

## Пример вывода

```
================================================================================
EDUSTREAM PLATFORM - TEACHER USAGE SCENARIO TESTING
================================================================================
API Base URL: https://94.131.85.176/api/v1
Started: 2024-02-09T16:06:53.528Z
================================================================================

🧪 Running Test 1: Authentication...
   → Attempting teacher registration...
   → Registration successful, attempting login...

✅ Authentication (Register & Login)
   Status: SUCCESS
   Details: Teacher can successfully register and login. JWT token received.
   Time: 2024-02-09T16:06:55.123Z

   🤖 Analyzing with AI...

...

================================================================================
FINAL TEST REPORT
================================================================================

📊 TEST STATISTICS:
   Total Tests: 8
   ✅ Successful: 6 (75.0%)
   ⚠️  Partial: 1 (12.5%)
   ❌ Failed: 1 (12.5%)

🤖 AI ANALYSIS SUMMARY:
   Average Usability Score: 7.8/10

================================================================================
OVERALL ASSESSMENT:
================================================================================

✅ PLATFORM IS READY FOR TEACHERS
   The EduStream platform demonstrates strong functionality across
   core teacher workflows...
```

## Интеграция с OpenRouter AI

Скрипт использует OpenRouter API для анализа результатов тестирования. API ключ встроен в скрипт, но вы можете заменить его на свой:

```javascript
const OPENROUTER_API_KEY = 'your-api-key-here';
```

Модель по умолчанию: `mistralai/mistral-small-3.1-24b-instruct:free`

## Архитектура тестов

Каждый тест:
1. Выполняет HTTP запрос к API
2. Проверяет статус ответа и данные
3. Логирует результат
4. Отправляет результат в OpenRouter AI для анализа
5. Получает оценку удобства использования и рекомендации

## Troubleshooting

### Ошибка подключения к API
```
Error: connect ECONNREFUSED
```
**Решение:** Проверьте, что бэкенд запущен и доступен по указанному URL.

### Ошибка с сертификатом SSL
```
Error: certificate verify failed
```
**Решение:** Скрипт уже настроен на работу с самоподписанными сертификатами (`rejectUnauthorized: false`).

### AI анализ недоступен
```
AI analysis failed: timeout
```
**Решение:** Проверьте интернет соединение и доступность OpenRouter API.

## Расширение тестов

Чтобы добавить новый тест:

1. Создайте функцию теста:
```javascript
async function testNewFeature(token) {
  // Ваш код теста
  return {
    scenario: 'New Feature Test',
    status: 'success',
    details: 'Feature works correctly',
    timestamp: new Date().toISOString()
  };
}
```

2. Добавьте вызов в `runAllTests()`:
```javascript
const newResult = await testNewFeature(token);
logResult(newResult);
results.push(newResult);

const analysis = await analyzeWithAI(newResult);
analyses.push(analysis);
```

## Результаты тестирования

Подробный отчет о тестировании доступен в файле [TEACHER_TEST_REPORT.md](./TEACHER_TEST_REPORT.md).

**Основные результаты:**
- ✅ 87.5% тестов успешны
- ⚠️ 12.5% требуют оптимизации
- 📊 Средняя оценка удобства: 8.4/10
- ✅ Платформа готова для использования учителями

## Лицензия

MIT License

## Контакты

Для вопросов и предложений: https://github.com/baltabekpro/-EduStream
